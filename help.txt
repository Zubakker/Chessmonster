что я буду делать с искусственным интеллектом?
искусственный интеллект -- это класс, в него должна передаваться доска
да. надо отдохнуть.


так.
в искусственный интеллект передаётся доска. он смотрит какие ходы он может сделать, да?
он должен делать ходы
поэтому
надо, чтобы была доска битости. или нет?
короче
есть доска битости где-то
когда противник просчитывает на 2 хода вперёд, он делает мысленно ход и у него появляется новая доска битости. значит он создаёт свою доску воображаемую вторую
Board() свой.

значит в Board надо сделать функцию загрузки не из файла, а из действующей доски.

кто просчитывает побитость? -- доска. Она проходит по каждой фигуре и у каждой фигуры должна быть функция "просчитать_побитость(кол-во очков хода)" или атака, хз. так
нужно, чтобы противник как-то понимал, выигрышный ли у него ход.
по очкам давления наверное. 
как расчитываются очки давления?
может быть есть алгоритм.


нужно:
1) генерация всех возможных ходов (сначала пусть будет без учёта очков на передвижение и с одной фигурой за ход)
2) пусть пока будет просто рандомный ход. мне ещё надо много подготовить заранее


так класс игрока:

итак. надо сделать что:
делается класс_игрока.сделать_ход(доска, игроки типа)
и только если класс 

когда идёт ход пользователя, тогда мы можем перемещать фигуры
двигать камеру мы можем всегда. если у нас игрок по интернету. тогда мы можем двигать камеру, если наступает ход интернет игрока, тогда типа интернет_игрок.сделать_ход(доска, айди) и когда мы делаем эту функцию, тогда оно отправляет запрос к другому игроку, но мы не будет оставлять ждать, потму что тогда у нас программа зависнет, пока он ходит, для этого нужны параллельные процессы или какие-то хендлеры кетчеры.


что я сейчас сделаю?: 
1)	таблица побитости
2)	посмотрим.
3)	надо сделать поддержку количества очков передвижения для всех фигур ух, вот это тяжело будет

итак. в доске побитости нужно для каждой клетки: если клетку бьют несколько фигур, то надо там хранить сразу несколько фигур. там должны быть координаты. а хотя зачем там хранить вообще кто бьёт эту клетку? Для перемещения фигур и постановки (в плане доски) побитость не важна. Для самих фигур, их валидации пути побитость не важна, для валидации постановки, побитость важна только королю, поэтому фигруы будут проврять побитость наверное только в постановке фигуры.


у piece должны быть функции return_attack( movement_points )  и validate_attack( map_path, board_path )
